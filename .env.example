# editor v2.0 云同步（Supabase）env 模板 —— 复制为 .env 填真值（.env 已 gitignore）。
#
# 不配（或留空）→ 同步禁用，editor 纯本地+离线照常（匿名零回归 / AC-v20-7）。
# 配齐 → 登录 + 云同步启用；supabase-js 此时才打包（懒加载 lazy chunk，不进首屏）。
#
# 这两个都是【公开】值（会进 FE bundle，正常）—— 真安全边界是 Supabase RLS（ADR-016），
# 不靠 anon key 保密。service_role key 等机密【绝不】放这里 / 进仓库 / 进 FE。
#
# 取值位置：Supabase 项目 → Settings → API → Project URL / Project API keys → anon public

VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
