# 清债 Consolidation — v0.9.1（2026-06-08）

**类型:** 技术债 consolidation（非功能版 / PATCH）；audit-driven（清既有 findings）
**审查 commit:** `a0efd2c`
**执行者:** 主会话（一人多角色，PR-001）

---

## 1. 背景

FE 功能井近干（v0.9.0 后），34 条未处理 findings 攒了 9 个版本（全 LOW，0 MEDIUM/HIGH）。本版**不加功能**，挑高价值子集清债降熵。明确**不做全清**——多数是 info/边缘/设计权衡，清了无价值还增回归面。

## 2. 清了什么（4 条）

| 编号 | 性质 | 处置 |
|------|------|------|
| **F-V11-3** | 旗舰 / **F-V11-1 家族漏网** | 静默吞错路径经 v1.6 重构从 M3.clear 迁到 **M9 用户操作 fire-and-forget store 写**（DocList `void m.x()`）。加 `guardStore`（log+toast），saveActiveText 仍由 M3 performWrite 接管（不双重处理）。**family scan 确认**静默路径仅此处 |
| **F-V12-2** | 正确性 | 导入加 `looksBinary`（NUL / >10% U+FFFD 替换字符）→ 二进制拒绝导入 + toast，不进编辑器乱码 |
| **F-V11-5** | dead code | 死 key `storage.unavailable`（v1.0 起未用）**复用**为 guardStore 通用 store 错误提示（一石二鸟：F-V11-3 要消息 + F-V11-5 死 key 复活）|
| **BHV-010** | 覆盖缺口 | 行号/字号/toast 此前仅 unit+手测 → 补 ac13 e2e（行号 toggle / 字号 13-15-17 档位 + 下限边界 / 复制 toast，双引擎）|

## 3. 明确丢了什么（defer + 理由）

| 不做 | 理由 |
|------|------|
| F-V16-4 多 tab | 需 BroadcastChannel/versionchange，非琐碎 + rare → 单独版本，不混进 consolidation |
| F-V16-1/F-V11-6 race(info) | 无实测问题，info 级不动 |
| F-V14-2 SVG XSS 单测 | jsdom 渲染不了 mermaid（PP-003 家族固有），e2e 已覆盖，硬补单测性价比低 |
| perf 类 4 条（F-V16-5/F-V18-1/BHV-008/F-V14-1）| 是 C 方向（需造大数据集压测），本次不碰 |
| UX 打磨 + 边缘/info ~15 条 | 接受现状或留后 |

## 4. 验证

- 单测 171（+looksBinary 4 / +remove-store-fail 1，guardStore IDB 失败 surface 实证）
- e2e：ac13 6 通过（双引擎）；full e2e 92 pass + 1 **已知 PP-003 #4 并行竞争 flake**（E2E-v16-002，隔离 5/5 过，非本次回归）
- 首屏 81.20KB；typecheck 0；doc-hash + fb 闸 pass

## 5. 结论

清 4 条（含旗舰 F-V11-3 + 流程债 family-scan 复核），剩 30 条多为 info/边缘/perf-待压测。无功能变更、无 DB 变更、无新 XSS 面（looksBinary 纯检测 / 重命名纯文本）。降熵动作，对得起 9 版攒的债的"利息"，不强行全清。

**过程一致性**：F-V11-3 印证 fix-pattern-scan 家族扫描价值——v1.1 当年只修了 loadStoredDocument（F-V11-1），clear 路径漏网（F-V11-3），且代码重构后静默点还会"迁移"，需重新 family scan 定位（不能照搬旧 finding 描述）。
