# v2.3 增量 audit — 代码块语法高亮

**日期：** 2026-06-12
**范围：** 共识 v2.3 / ADR-019 ↔ 实现 `f966af3`，增量 only
**基线：** unit 230 → 238（+8）；e2e 116 → 122 + 2 skip；首屏 85.93 → 86.47KB gz；precache 1405 → 1570KB（hljs chunk 162KB raw 进 precache，离线高亮可用，同 katex 策略）

---

## 1. Spec ↔ 实现对齐

| AC | 实证 | 结论 |
|----|------|------|
| AC-v23-1 常用语言着色 | CT-HL-3 + E2E-AC16-1 双引擎 | ✅ |
| AC-v23-2 未知/无标注降级 | CT-HL-2/4/5 | ✅ |
| AC-v23-3 mermaid 不被拦截 | CT-HL-6 + 既有 ac9 回归 | ✅ |
| AC-v23-4 **XSS 门槛** | CT-HL-7 + E2E-AC16-2 双引擎（script/onerror/js: 全剥离无 alert）| ✅ |
| AC-v23-5 lazy chunk 首屏不增 | size 闸 86.47/150 | ✅ |
| AC-v23-6 主题即时跟随 | E2E-AC16-3（computed color 切换实证，零重渲染）| ✅ |
| AC-v23-7 零回归 | 既有全量 e2e 122 pass | ✅ |

## 2. 安全面（本版核心审查点）

- **sanitize 不放宽**：render() 的 DOMPurify 配置零改动（仍仅 ADD_ATTR data-source-line）；hljs 输出 span+class 走默认 allowlist ✅
- hljs `highlight()` 对非 token 文本自身 escape；闭包 try/catch + `ignoreIllegals` 双兜底 ✅
- 加载失败（动态 import 拒绝）→ hljsLib 保持 null → 永久降级无色，不崩 ✅

## 3. 实现期发现并处置

| 发现 | 处置 |
|------|------|
| hasCode 正则两次写出多余 `*` 量词（fence 变可选 → 任意文本误判 true）| 实现期自查修正 + CT-HL-1 启发式族单测锁死；误判后果本就无害（多加载一次），但修正避免全量用户加载 162KB chunk |

## 4. 新增 findings

| 编号 | severity | 说明 |
|------|----------|------|
| F-V23-1 | LOW (info) | hljs chunk（162KB raw）进 SW precache，离线高亮可用（katex 同策略）；precache 累计 1.57MB，距 F-V15-1 修订时 1.18MB 增 33%，再有重 chunk 时应重评 runtimeCaching 分流 |
| F-V23-2 | LOW (info) | 自绘 9 组 token 映射较 hljs 官方主题粗（scope 归并），少数语言细分 token 沿用 --fg 默认色；视觉打磨 defer 观察反馈 |

## 5. 结论

**无 critical/high/medium。** 安全红线（ADR-002 不放宽 sanitize）经 unit + e2e 双层 XSS 实证守住；降级路径全枚举。可进入 tag v1.3.0-rc.1。
