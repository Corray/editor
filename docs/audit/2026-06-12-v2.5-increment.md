# v2.5 增量 audit — 打印 / 导出 HTML

**日期：** 2026-06-12
**范围：** 共识 v2.5 / ADR-021 ↔ 实现 `c270057`，增量 only
**基线：** unit 249 → 255（+6）；e2e 131+3skip → 134+3skip（+2 用例）；首屏 87.61 → 88.77KB gz

---

## 1. Spec ↔ 实现对齐

| AC | 实证 | 结论 |
|----|------|------|
| AC-v25-1 print 媒体 chrome 全隐+浅色 | E2E-AC18-2 双引擎（emulateMedia + dark 下 --bg=#ffffff 实证）| ✅ |
| AC-v25-2/6 下载+文件名+自包含 | CT-XH-1 + E2E-AC18-1（download 事件 + 文件内容断言）| ✅ |
| AC-v25-3 mermaid SVG 保真 | CT-XH-4（svg 保留）+ 内容源 = 预览 DOM | ✅ |
| AC-v25-4 **二次 sanitize** | CT-XH-2/3（script/onerror/foreignObject/data-source-line 全剥）+ E2E-AC18-1 | ✅ |
| AC-v25-5 既有导出零回归 | 既有 ac3/ac7 全过 | ✅ |

## 2. 安全面

- 导出产物 = 双重 sanitize（渲染时一次 + 导出时 FORBID foreignObject/script 再一次）+ 内部属性剥离 ✅
- KaTeX CDN link 仅入导出产物，**SRI 从本地 node_modules 同版本文件 openssl 计算**（不引构建期网络依赖）；应用本体 CSP/自托管零变化 ✅
- 风险边界：CDN 域（jsdelivr）若停服 → 导出文件公式排版降级（结构在）；SRI 锁内容防篡改 ✅

## 3. 附带处置

| 事项 | 处置 |
|------|------|
| F-V22-1（ac15-2 webkit 预览联动 poll flake）**二次复发** | 达复发阈值 → 加固（poll 显式 10s，负载下 rAF 链路慢的环境噪声）；状态升级见 registry |

## 4. 新增 findings

| 编号 | severity | 说明 |
|------|----------|------|
| F-V25-1 | LOW (info) | 导出 HTML 的 KaTeX 样式依赖 CDN（离线打开排版降级，结构在）——TBD-v25-3a 拍定取舍，文档化 |
| F-V25-2 | LOW (info) | 移动端编辑 tab 导出走降级源（render(text)，mermaid 为占位源码）；预览 tab 下与桌面一致。边缘场景，文档化 |
| F-V25-3 | LOW (info) | print 在移动编辑 tab 下预览未挂载 → 打印空内容；桌面/预览 tab 正常。边缘，defer |

## 5. 结论

**无 critical/high/medium。** 导出安全面双重 sanitize 实证；print 强制浅色 + 滚动容器解除经 e2e 双引擎验。可进入 tag v1.5.0-rc.1。
