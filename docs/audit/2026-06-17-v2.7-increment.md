# v2.7 增量 audit — Markdown 格式工具栏

**日期：** 2026-06-17
**范围：** 共识 v2.7 / ADR-023 ↔ 实现 `c3139d8`，增量 only
**基线：** unit 263 → 274（+11）；e2e 138+3skip → 148+4skip（+5 用例，+1 webkit undo skip）；首屏 90.52 → 91.19KB gz

---

## 1. Spec ↔ 实现对齐

| AC | 实证 | 结论 |
|----|------|------|
| AC-v27-1 B/I/code toggle | CT-CODE×3 + E2E-AC20-1/2 双引擎 | ✅ |
| AC-v27-2 链接占位 | 既有 CT-FMT-8 回归 + E2E-AC20-2 | ✅ |
| AC-v27-3 引用/无序/有序前缀+递增 | CT-LP-1/2/3 + E2E-AC20-3 | ✅ |
| AC-v27-4 行前缀 toggle 去除 | CT-LP-4/5/6 | ✅ |
| AC-v27-5 代码块围栏 | CT-CB×2 + E2E-AC20-2 | ✅ |
| AC-v27-6 工具栏 undo | E2E-AC20-4 chromium（F-V21-1 同限）| ✅ |
| AC-v27-7 移动端可见可点 | E2E-AC20-5（320px viewport）| ✅ |
| AC-v27-8 零回归 | 全量 e2e 148 一次过 | ✅ |

## 2. 复用率（本版核心特征）

- 4/8 按钮（粗/斜/码/链）直接走既有 `applyFormat`（v2.1 ADR-017）；新代码仅 `toggleLinePrefix` + `wrapCodeBlock` + FormatToolbar 组件
- 全部经 `replaceRange` → undo 保持（AC-v27-6 实证）；行前缀复用 indentSelection 的覆盖行 + 选区重算范式

## 3. 实现期要点

| 事项 | 处置 |
|------|------|
| 点工具栏按钮夺 textarea 焦点 → selection 丢失 → 命令作用于空选区 | FormatToolbar 按钮 `onMouseDown` preventDefault（不转移焦点）+ 操作后回焦 |
| noUncheckedIndexedAccess 下 Record 索引 `T\|undefined` | 去掉中间 map，按钮回调直接传字面量 FormatKind/LinePrefixKind |

## 4. 安全面

- 工具栏插入文本只进 textarea value（纯文本 marker/前缀/围栏）→ 渲染走既有 DOMPurify 管线，不动 sanitize ✅
- 无新外部输入面 ✅

## 5. 新增 findings

| 编号 | severity | 说明 |
|------|----------|------|
| F-V27-1 | LOW (info) | 工具栏 glyph 用 Unicode 字符（B/I/<>/🔗/"/•/1./{ }），非图标字体；视觉朴素，移动端 🔗 emoji 渲染随系统。视觉打磨 defer |
| F-V27-2 | LOW (info) | 行前缀 toggle「部分行带前缀 → 补齐为全加」，无「部分态」第三档；直觉一致（与 B/I 同），但重度用户可能期望先统一再切。MVP 接受 |

## 6. 结论

**无 critical/high/medium。** 高复用（半数按钮走既有命令），新代码经既有 replaceRange/选区范式控住，undo + 移动可见 e2e 实证。2 LOW info。可进入 tag v1.7.0-rc.1。
