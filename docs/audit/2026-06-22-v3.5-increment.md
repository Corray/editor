# v3.5 增量 audit — callout 容器块

**日期：** 2026-06-22
**范围：** 共识 v3.5 / ADR-031 ↔ 实现 `adbf5e5`，增量 only
**基线：** unit 330 → 337（+7）；e2e 190+4skip → 194+4skip（+2 用例）；首屏 96.00 → 96.35KB gz

---

## 1. Spec ↔ 实现对齐

| AC | 实证 | 结论 |
|----|------|------|
| AC-v35-1 4 类 callout | CT-CALLOUT-1 + E2E-AC28-1 双引擎 | ✅ |
| AC-v35-2 自定义/默认标题 | CT-CALLOUT-2/3 + E2E-AC28-1 | ✅ |
| AC-v35-3 内部 markdown | CT-CALLOUT-4 + E2E-AC28-1（strong）| ✅ |
| AC-v35-4 未知类型不渲染 | CT-CALLOUT-5 | ✅ |
| AC-v35-5 lazy chunk 首屏不增 | size 闸 96.35（并入扩展链）| ✅ |
| AC-v35-6 **XSS 门槛** | CT-CALLOUT-6 + E2E-AC28-2 双引擎 | ✅ |
| AC-v35-7 零回归 | 全量 e2e 194 一次过 | ✅ |

## 2. 安全面

- `<div class>` + 标题 escapeHtml + 内部正常 markdown（render() DOMPurify）；div/class 默认放行；ADD_ATTR 不动（ADR-002）；XSS unit+e2e 双引擎验 ✅
- 并入 v3.4 ensureExtensions 链：applyExtensions 仍每实例一次（baseMd+katexMd 对称协同未变）✅

## 3. 实现期发现并处置

| 事项 | 处置 |
|------|------|
| CT-CALLOUT-3 默认类型名断言 jsdom navigator en-US → 't' 返英文（PP-006 同源）| 测试显式 setLang zh/en 双验（环境默认须显式置基线，PP-006 教训复用）|

## 4. 新增 findings

| 编号 | severity | 说明 |
|------|----------|------|
| F-V35-1 | LOW (info) | callout 默认类型名标题用 t() 在 render 期取值 → 语言切换不重渲染该标签（仅文本变更时更新）；自定义标题不受影响，影响微 |
| F-V35-2 | LOW (info) | 不支持嵌套 callout / 用户自定义类型（markdown-it-container 固定 4 类），范围内文档化 |

## 5. 结论

**无 critical/high/medium。** 并入扩展链不破双实例同步；安全默认放行 + 标题 escapeHtml + XSS 双引擎验。2 LOW info。可进入 tag v1.15.0-rc.1。
