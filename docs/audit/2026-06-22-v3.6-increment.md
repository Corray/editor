# v3.6 增量 audit — 文本高亮/标记（mark/ins）

**日期：** 2026-06-22
**范围：** 共识 v3.6 / ADR-032 ↔ 实现 `6a3a889`，增量 only
**基线：** unit 337 → 342（+5）；e2e 194+4skip → 197+4skip（+2 用例）；首屏 96.35 → 96.40KB gz

---

## 1. Spec ↔ 实现对齐

| AC | 实证 | 结论 |
|----|------|------|
| AC-v36-1 `==`→mark | CT-MARK-1 + E2E-AC29-1 双引擎 | ✅ |
| AC-v36-2 `++`→ins | CT-MARK-2 + E2E-AC29-1 | ✅ |
| AC-v36-3 lazy 首屏不增 | size 闸 96.40（并入扩展链）| ✅ |
| AC-v36-4 未载降级 | hasExtension-0（未触发不渲染）| ✅ |
| AC-v36-5 **XSS 门槛** | CT-MARK-3 + E2E-AC29-2 双引擎 | ✅ |
| AC-v36-6 零回归（删除线 ~~）| CT-MARK-4（`<s>` 核心未受影响）+ 全量 e2e | ✅ |

## 2. 安全面

- `<mark>/<ins>` DOMPurify 默认放行（标准标签）；经 render() sanitize；ADD_ATTR 不动（ADR-002）；XSS unit+e2e 双引擎验 ✅
- 并入扩展链不破双实例同步（applyExtensions 仍每实例一次）✅

## 3. 实现期

- ac5-perf E2E-AC5-002 全量回归 flake（隔离 4/4 过）——负载假阴性（历轮 ac5 一致），非 v3.6 回归

## 4. 新增 findings

无（极小插件，无新限制；mark/ins 标准标签默认放行，删除线核心不受影响）。

## 5. 结论

**无 critical/high/medium / 无新 finding。** 极小插件并入链不破同步 + 安全默认放行 + XSS 双引擎验。可进入 tag v1.16.0-rc.1。
