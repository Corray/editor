# v3.7 增量 audit — 主题增强（强调色）

**日期：** 2026-06-22
**范围：** 共识 v3.7 / ADR-033 ↔ 实现 `02003c5`，增量 only
**基线：** unit 342 → 347（+5）；e2e 197+4skip → 202+4skip（+2 用例）；首屏 96.40 → 97.01KB gz

---

## 1. Spec ↔ 实现对齐

| AC | 实证 | 结论 |
|----|------|------|
| AC-v37-1 选色 → accent 即时变 | CT-SET-8 + E2E-AC30-1（--accent computed=#16a34a）双引擎 | ✅ |
| AC-v37-2 持久化 | CT-SET-8 + E2E-AC30-1（reload 保留）| ✅ |
| AC-v37-3 浅深各有值 | variables.css 4×2 规则（CSS 静态，人工核 + e2e 浅色实测）| ✅ |
| AC-v37-4 默认蓝零变化 | CT-SET-7/9 + E2E-AC30-2（blue→删 data-accent）| ✅ |
| AC-v37-5 坏值回退 | CT-SET-10/11 | ✅ |
| AC-v37-6 零回归 | 全量 e2e 202 一次过（主题/设置其他项/语言）| ✅ |

## 2. 设计正确性

- **正交 M6**：data-accent 独立于 data-theme，两维度互不干扰（E2E-AC30 在默认 light 下验，深色规则静态存在）
- **默认零变化**：accentColor='blue' → 删 data-accent → 用 variables.css 默认 --accent（蓝），既有行为不变（CT-SET-9 实证）
- anti-poisoning 复用 v2.9 settings 范式（非档位回 blue）

## 3. 安全面

- accent 值枚举校验（ACCENT_PRESETS）；CSS 变量驱动；无外部输入面 / 无 innerHTML ✅

## 4. 新增 findings

无（cosmetic + 枚举校验 + 复用 settings 持久化范式；无新限制除"仅 5 预设/不加整套主题"已在共识范围声明）。

## 5. 结论

**无 critical/high/medium / 无新 finding。** 正交 M6 + 默认零变化 + anti-poisoning 复用。可进入 tag v1.17.0-rc.1。

**第四批 scope 全部交付**（v3.5 callout / v3.6 mark-ins / v3.7 主题增强）。
